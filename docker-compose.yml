version: '3'
services:
              web:
                      image: flask
                      build: .
                      env_file:
                              - .env
                      ports:
                              - "8300:8300"
                      volumes:
                              - ./instance:/instance
                      restart: unless-stopped
